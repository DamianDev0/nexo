import { Meta } from '@storybook/addon-docs/blocks'

import { SPACE_SCALE } from '../shared/config/tokens/design-scale'
import { ScaleTable } from './ScaleTable'

<Meta title="Foundations/Spacing" />

# Spacing

Base 4px. Card padding 24–28px. Gap between cards 16–20px. Page content
padding 36px horizontal. Sidebar 268px with 16px inner padding. Density is
comfortable: rows 64px, fields 52px, buttons 48px, nav items 46px.

<ScaleTable rows={SPACE_SCALE} />
